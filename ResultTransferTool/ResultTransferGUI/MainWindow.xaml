<Window x:Class="ResultTransferGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ResultTransferGUI"
        xmlns:userControl ="clr-namespace:ResultTransferGUI.UserControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="640" Width="920" WindowStyle="ThreeDBorderWindow"
        Icon="logo/Windows_Easy_Transfer.ico"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Loaded="MainWindow_OnLoaded">
    <Window.Resources>
        <Style TargetType="Rectangle" x:Key="PrettyRectangle">
            <Setter Property="RadiusX" Value="8"></Setter>
            <Setter Property="RadiusY" Value="8"></Setter>
            <Setter Property="Margin" Value="3"></Setter>
            <Setter Property="Fill" Value="AliceBlue"></Setter>
        </Style>
    </Window.Resources>
    <Grid  Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Rectangle Grid.Row="0" Grid.ColumnSpan="2" Style="{StaticResource PrettyRectangle}"></Rectangle>
        <Image Margin="10" Grid.Row="0" Grid.Column="0" Source="/ANDREW-logo_250x47.png"></Image>
        <DockPanel Grid.Row="1" Grid.RowSpan="2" Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Rectangle Grid.Row="0" Grid.RowSpan="2" Style="{StaticResource PrettyRectangle}"></Rectangle>
                <ListBox Margin="6" Grid.Row="1" BorderBrush="Transparent" ItemsSource="{Binding FileViewModels}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding FileName}"></TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="{Binding Background}"></Setter>
                            <Setter Property="Foreground" Value="{Binding Foreground}"></Setter>
                            <Setter Property="ToolTip" Value="{Binding ToolTip}"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
                <Label Grid.Row="0" HorizontalContentAlignment="Center"
                       VerticalContentAlignment="Bottom"  Content="Result Files"
                       />
            </Grid>
        </DockPanel>
        <Grid Grid.Column="1" Grid.Row="1">
            <Rectangle  Style="{StaticResource PrettyRectangle}" ></Rectangle>
            <StackPanel></StackPanel>
        </Grid>
        <Grid Grid.Column="1" Grid.Row="2">
            <Rectangle Style="{StaticResource PrettyRectangle}" ></Rectangle>
            <DockPanel LastChildFill="True">
                <userControl:LogViewer Margin="10" BorderBrush="Transparent"></userControl:LogViewer>
            </DockPanel>
        </Grid>
        <Grid Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2">
            <Rectangle Style="{StaticResource PrettyRectangle}" > </Rectangle>
            <StackPanel/>
        </Grid>
    </Grid>
</Window>
