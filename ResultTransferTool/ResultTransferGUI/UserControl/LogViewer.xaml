<UserControl x:Class="ResultTransferGUI.UserControl.LogViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ResultTransferGUI.UserControl"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             >

    <Grid>
        <ListView ItemsSource="{Binding LogEntries}" Name="LogView">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="ToolTip" Value="{Binding ToolTip}"></Setter>
                    <Setter Property="Background" Value="{Binding Background}"></Setter>
                    <Setter Property="Foreground" Value="{Binding Foreground}"></Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="DarkOrange"></Setter>
                            <Setter Property="Foreground" Value="Black"></Setter>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{Binding (local:LogEventViewModel.BackgroundMouseOver)}"></Setter>
                            <Setter Property="Foreground" Value="{Binding (local:LogEventViewModel.ForegroundMouseOver)}"></Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn DisplayMemberBinding="{Binding Time}" Header="Time"></GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Category}" Width="80" Header="Category"></GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Level}" Width="50" Header="Level"></GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Message}" Width="500" Header="Message"></GridViewColumn>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
